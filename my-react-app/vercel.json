{
  "rewrites": [
    { "source": "/api",        "destination": "https://<YOUR-BACKEND>.vercel.app/api" },
    { "source": "/api/:path*", "destination": "https://<YOUR-BACKEND>.vercel.app/api/:path*" }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
