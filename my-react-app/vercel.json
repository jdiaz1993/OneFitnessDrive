{
  "routes": [
    { "src": "^/api$", "dest": "https://YOUR-BACKEND.vercel.app/api" },
    { "src": "^/api/(.*)$", "dest": "https://YOUR-BACKEND.vercel.app/api/$1" },

    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}