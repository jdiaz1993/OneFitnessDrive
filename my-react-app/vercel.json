{
   "rewrites": [
    { "source": "/api/(.*)", "destination": "https://YOUR-BACKEND.vercel.app/api/$1" }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}